/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // appDir: true, // Supprimez ou commentez cette ligne
  },
};

module.exports = nextConfig;
